
find /etc -type f -print | xargs grep (検索文字列) /dev/null


=============================
find コマンドで /etc 以下の通常のファイル(ディレクトリなど以外)を探し(-type f)、ファイル名を表示させます(-print)。
xargs コマンドは find の出力したファイル名を展開しつつ、

grep hoge /dev/nlll ファイル名リスト

というコマンドを実行します。

